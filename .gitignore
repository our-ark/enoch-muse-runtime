# Runtime state: the live file mailbox (inbox/outbox traffic).
# Never commit live requests/replies.
mailbox/
__pycache__/

*.egg-info/
.venv/
build/
dist/
.enoch/
.env
.env.*
!.env.example
*.pem
*.key

# Group room runtime state (live transcripts/archives) — never commit.
group/staged/
group/room.json
group/room.md
group/exchange.active
